/// Centralized endpoint paths for the force update feature.
/// All paths are relative (no leading slash, no version prefix).
class AppVersionEndpoints {
  const AppVersionEndpoints._();

  static const String latestVersion = 'app-versions/latest';
}

/// Centralized endpoint paths for the certifications feature.
/// All paths are relative (no leading slash, no version prefix).
class CertificationEndpoints {
  const CertificationEndpoints._();

  static const String certifications = 'certifications';
  static const String myCertification = 'certifications/my';
}
